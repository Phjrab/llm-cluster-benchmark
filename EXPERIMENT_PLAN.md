# 라즈베리 파이 · Jetson Orin Nano LLM 클러스터 비교 실험

## 목적과 가설

이 실험은 두 플랫폼에서 경량 LLM을 실행했을 때의 **단일 노드 추론 성능**과
**여러 노드가 동시에 처리할 수 있는 요청량**을 비교한다.  클러스터는 모델을
여러 장비에 쪼개는 구성이 아니라, 각 노드가 모델 사본 하나를 가지고 요청을
분산 처리하는 구성을 뜻한다.

- H1: 같은 모델·양자화에서는 Jetson Orin Nano가 Raspberry Pi보다 prompt 처리와 생성 속도가 높다.
- H2: 노드를 1→2→4대로 늘리면 단일 요청 지연은 크게 개선되지 않지만, 동시 요청 처리량은 증가한다.
- H3: Raspberry Pi는 1B~3B, Jetson은 3B~8B 4-bit 모델에서 실용적인 성능 구간을 보인다.

## 권장 장비 및 소프트웨어

| 구분 | Raspberry Pi | Jetson Orin Nano |
|---|---|---|
| 노드 | 동일 사양 1, 2, 4대 | 동일 사양 1, 2, 4대 |
| 연결 | Gigabit Ethernet 스위치, 유선만 사용 | 동일 |
| 냉각 | 능동 냉각, 실험 전 동일 fan 설정 | 동일 |
| 런타임 | `llama.cpp` CPU 빌드 | CUDA를 활성화한 `llama.cpp` |
| 모델 | Qwen2.5-1.5B/3B, Llama-3.2-1B/3B GGUF Q4_K_M | 같은 모델 + Qwen2.5-7B 또는 Llama-3.1-8B Q4_K_M |

각 노드에 동일 버전의 `llama.cpp`와 동일한 GGUF 파일을 둔다. Jetson은 `-ngl 999`
같은 GPU offload 설정을 기록하고, Pi는 CPU thread 수(`-t`)를 기록한다.

## 실험 설계

### 1. 단일 노드 microbenchmark

`llama-bench`를 사용해 아래 입력/출력 조합을 노드당 5회 반복한다.

| 입력 토큰 (`pp`) | 생성 토큰 (`tg`) | 의미 |
|---:|---:|---|
| 128 | 128 | 짧은 질의응답 |
| 512 | 128 | 문서 요약형 입력 |
| 512 | 512 | 긴 생성 |

기록값: prompt tok/s (`pp`), generation tok/s (`tg`), RAM, 평균 전력(W), 온도(°C).
각 반복 사이에는 30초 이상 쉬어 열 누적을 줄인다. 결과는 평균뿐 아니라 중앙값과
표준편차도 보고한다.

### 2. 단일 요청 end-to-end

동일 프롬프트와 `max_tokens=128`로 API 서버에 5회 요청한다.

- TTFT: 요청 전송부터 첫 스트리밍 토큰까지 시간
- E2E: 요청 전송부터 마지막 토큰까지 시간
- 생성 tokens/s: 생성 토큰 수 / (E2E - TTFT)

### 3. 클러스터 부하 실험

각 노드에서 `llama-server`를 기동하고, 프록시(nginx/HAProxy 등)의 round-robin
백엔드에 1·2·4대를 순서대로 등록한다. 이 저장소의 `load_test.py`를 다음 조합으로 실행한다.

| 노드 수 | 동시 요청 수 | 총 요청 수 |
|---:|---:|---:|
| 1, 2, 4 | 1, 2, 4, 8 | 각 조합 20 |

보고 지표는 전체 생성 tok/s, 성공률, p50/p95 E2E 지연, p50/p95 TTFT, 요청당 에너지(J)다.

## 통제 변수와 공정성

- 모델 파일, 양자화, context 길이, 프롬프트, seed, 온도·top-p·max tokens를 고정한다.
- 같은 네트워크와 API 프록시를 두 플랫폼에 사용하고, 클라이언트는 별도 PC에서 실행한다.
- 백그라운드 서비스·전원 모드·냉각 조건을 기록한다. Jetson은 `nvpmodel`/클럭 모드도 기록한다.
- 모델이 메모리에 완전히 올라가는지 확인한 뒤 워밍업 3회를 하고 측정을 시작한다.
- 전력 측정기는 노드별 입력 전원에서 측정한다. 다중탭 전체 전력만 측정했다면 그렇게 명시한다.

## 실행 순서

1. 각 노드에서 `llama.cpp`를 빌드하고 모델을 배치한다.
2. `scripts/run_llama_bench.sh`의 `MODEL`, `LLAMA_BENCH`, `EXTRA_ARGS`를 수정해 노드마다 실행한다.
3. 각 노드에서 예: `./llama-server -m MODEL.gguf -c 2048 --port 8080 ...`를 실행한다.
4. 프록시 주소를 `load_test.py`에 넘겨 부하 실험을 실행한다.
5. 생성된 CSV들을 합쳐 노드 수·동시성별 평균, p50, p95를 그래프로 만든다.

## 결과 표 템플릿

| 플랫폼 | 모델 | 노드 | 동시성 | pp tok/s | tg tok/s | TTFT p50/p95 (s) | E2E p50/p95 (s) | 전체 tok/s | 평균 W | °C |
|---|---|---:|---:|---:|---:|---|---|---:|---:|---:|
| Raspberry Pi | Qwen 3B Q4 |  |  |  |  |  |  |  |  |  |
| Jetson Orin Nano | Qwen 3B Q4 |  |  |  |  |  |  |  |  |  |
